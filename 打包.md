
# WPF应用打包

有很多种方式, 但最常用的有以下两种:

## ClickOnce打包

如果应用不需要在微软商店发布, 只是内部使用, 则可以使用ClickOnce打包. 具体步骤如下:

在项目上右键, 选择"发布", 然后按照向导完成发布即可.太简单了!

## MSIX打包

如果应用需要在微软商店发布, 则需要使用MSIX打包.

MSIX打包也有很多种方式, 这里介绍在Visual Studio中"使用Windows应用打包项目"的方式. 具体步骤如下:
### 1.创建打包项目

在现有WPF解决方案中，右键点击解决方案 → 添加​ → 新建项目

搜索"Windows Application Packaging Project"或者"windows应用打包项目"，选择该模板，点击下一步。
为打包项目指定名称，例如"MyApp.Package"，然后点击创建。

### 2. 添加应用程序引用

在打包项目上右键, 选择"添加引用", 然后选择你的WPF项目.


### 3. 配置打包项目属性

编辑Package.appxmanifest, 配置打包项目的属性, 包括包名, 版本号, 发布者等信息.

### 4. 生成配置

如果没有证书就不要动.

编辑.csproj文件(双击打包项目),添加以下配置
```xml
<PropertyGroup>
  <!-- 生成MSIX包而不是应用包 -->
  <GenerateAppInstallerFile>False</GenerateAppInstallerFile>
  <!-- 签名 -->
  <AppxPackageSigningEnabled>True</AppxPackageSigningEnabled>
  <!-- 证书指纹 -->
  <PackageCertificateThumbprint>YOUR_CERT_THUMBPRINT</PackageCertificateThumbprint>
  <!-- 可选：启用自动增量版本 -->
  <AppxAutoIncrementPackageRevision>True</AppxAutoIncrementPackageRevision>
  <AppxSymbolPackageEnabled>False</AppxSymbolPackageEnabled>
</PropertyGroup>
```

### 5. 生成MSIX包

1. 在解决方案资源管理器中，右键点击打包项目
2. 选择 发布​ → 创建应用包
	3. 在向导中选择：
	旁加载(因为没有微软开发人员账户和证书)
	输出位置
	版本号
	目标平台（x86/x64/ARM64）
	生成包到单个文件或多个文件
    填写"安装程序位置",指的是WPF主项目的输出位置，不是安装程序的存放位置,比如:
	D:\workspace-learn\cross-platform-application\windows-tools\eye-guard\eye-guard\bin\Release\net8.0-windows\publish
4. 点击"创建"


生成打包项目, 生成的MSIX文件位于打包项目的输出目录下.
. 如果需要发布到微软商店, 则需要在微软合作伙伴中心注册开发者账号, 并创建应用, 然后上传MSIX文件进行审核.
. 审核通过后, 应用即可在微软商店发布.
. 用户可以通过微软商店下载安装应用, 也可以通过直接安装MSIX文件的方式安装应用.