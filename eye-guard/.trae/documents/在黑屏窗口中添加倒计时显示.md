1. 在ScreenController类中添加一个新的List<TextBlock>字段，用于存储每个黑屏窗口的倒计时文本控件
2. 在构造函数中初始化该列表
3. 修改CreateBlackoutWindow方法，为每个窗口添加居中的倒计时文本控件：
   - 创建TextBlock控件
   - 设置字体大小、颜色和居中对齐
   - 将TextBlock设置为窗口内容
   - 将TextBlock添加到新列表中
4. 修改OnBlackoutTimerTick方法，在倒计时更新时同步更新所有窗口的文本
5. 在EndBlackout方法中清空倒计时文本控件列表
6. 添加必要的using指令（System.Windows.Controls）