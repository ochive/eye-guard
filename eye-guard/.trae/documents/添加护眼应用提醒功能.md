## 实现步骤

### 1. 修改 TimerManager.cs
- 添加 `TimeWarning` 事件，用于在黑屏前1分钟触发提醒
- 在 `OnTimerElapsed` 方法中，当剩余时间为1分钟时触发该事件

### 2. 修改 MainWindow.xaml.cs
- 订阅 `TimerManager` 的 `TimeWarning` 事件，显示"即将黑屏休息，请注意"的弹窗
- 修改 `PauseTimer` 事件处理程序中的提示信息，改为"已经将休息时间推迟1分钟"

### 3. 验证修改
- 确保提醒功能在剩余1分钟时正确触发
- 确保推迟休息时显示正确的提示信息

## 技术细节
- 使用现有的 `ShowMessage` 方法显示弹窗
- 保持代码风格与现有代码一致
- 确保事件处理在UI线程中执行